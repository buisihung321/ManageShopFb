@model IEnumerable<ManageShop.Models.OrderDetail>
@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<table id="orderDetails" class="table table-hover table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Quantity</th>
            <th>Unit Price</th>
            <th>Order Id</th>
            <th>Amount</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var od in Model)
        {
        <tr>
            <td>@od.Id</td>
            <td>@od.Quantity</td>
            <td>@od.UnitPrice</td>
            <td>@od.OrderId</td>
            <td>@od.Amount</td>
        </tr>
        }
    </tbody>
</table>

